services:
  - type: web
    name: linkguard-ml
    env: docker
    dockerContext: ./ml-service
    dockerfilePath: Dockerfile
    plan: free
    region: ohio
    envVars:
      - key: LOG_LEVEL
        value: INFO
