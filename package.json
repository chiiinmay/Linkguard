{
  "name": "linkguard-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend && pip install -r ml-service/requirements.txt",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"cd ml-service && uvicorn main:app --port 8000 --reload\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
